# AGENTS.md

Read `CLAUDE.md` first. It is the repo contract for Budzie.

Important:

- Product name is Budzie everywhere.
- Private source material is local-only. Do not stage paths matched by
  `.git/info/exclude`.
- Never write private source names or comparisons into tracked files, commits, or
  PR text.
- Use `budzie:` markers for deliberate shortcuts.
- Keep changes local-first, minimal, and verified.
